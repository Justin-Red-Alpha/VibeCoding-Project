fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
requests==2.32.3
beautifulsoup4==4.12.3
apscheduler==3.10.4
python-multipart==0.0.9

# Renders JavaScript-only shops and runs the per-shop site search.
# Lazada will NOT work without it (its server HTML only carries the list price).
# After installing: python -m playwright install chromium
playwright==1.63.0

# Postgres, used only when DATABASE_URL is set (the hosted site). Local runs use
# the SQLite file and never import these. The binary extra bundles libpq.
psycopg[binary]==3.3.6
psycopg-pool==3.3.3
